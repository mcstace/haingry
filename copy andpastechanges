body: SafeArea(
  child: Padding(
    padding: const EdgeInsets.all(24.0),
    child: Column(
      mainAxisAlignment: MainAxisAlignment.center,  // ← This centers everything!
      crossAxisAlignment: CrossAxisAlignment.center,  // ← This too
      children: [
        // Logo at the very top (pull it up out of center)
        Align(
          alignment: Alignment.topCenter,
          child: Padding(
            padding: const EdgeInsets.only(bottom: 60),
            child: SizedBox(
              height: 40,  // Make it smaller
              child: Image.asset(
                'assets/images/haingrypurple.png',
                fit: BoxFit.contain,
                errorBuilder: (context, error, stackTrace) {
                  return const Text(
                    'haingry',
                    style: TextStyle(
                      fontSize: 32,
                      fontWeight: FontWeight.bold,
                      color: Color(0xFF7B68EE),
                    ),
                  );
                },
              ),
            ),
          ),
        ),
        
        // Fridge Illustration
        Container(
          width: 220,
          height: 340,
          // ... your fridge code
        ),
        
        const SizedBox(height: 40),
        
        // Camera Button
        GestureDetector(
          // ... your camera button code
        ),
        
        const SizedBox(height: 24),
        
        // Title
        const Text(
          'Take a photo of\nyour fridge!',
          style: TextStyle(
            fontSize: 32,
            fontWeight: FontWeight.bold,
            color: Colors.black,
            height: 1.2,
          ),
          textAlign: TextAlign.center,
        ),
      ],
    ),
  ),
),
